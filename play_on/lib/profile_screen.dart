import 'package:flutter/material.dart';

class Profile_sceen extends StatelessWidget {
  const Profile_sceen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Profile"),
        ),
    );
  }
}